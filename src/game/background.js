export function update(dt, state) {
  // Background ticket #2 implements the parallax scrolling layers.
}

export function draw(ctx, state) {
  // Background ticket #2 implements the sky, castle and hills.
}
