export function getHighscore() {
  // Highscore ticket #4 implements the localStorage read and validation.
  return 0;
}

export function saveHighscore(score) {
  // Highscore ticket #4 implements the localStorage write.
  return 0;
}
