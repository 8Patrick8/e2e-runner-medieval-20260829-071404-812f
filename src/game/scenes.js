export function update(dt, state) {
  // Scenes ticket #7 implements the start/gameover transitions and restart.
}

export function draw(ctx, state) {
  // Scenes ticket #7 implements the start and gameover overlays.
}
