export function update(dt, state) {
  // Obstacles ticket #1 implements spawning, movement and collision.
}

export function draw(ctx, state) {
  // Obstacles ticket #1 implements the barrel and fence sprites.
}
